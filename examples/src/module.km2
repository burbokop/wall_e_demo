
_ = namespace {

    printf = (format string, args ...) u32;
    my_cst = 'asas';
    pow = (a f64, b f64) f64;

	exp = namespace {
		print_hello = () {
    	    printf('hello goga\n');
	    };    
    };

	//exp = 'aaaaaa';
};
