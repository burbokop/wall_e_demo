
_ = namespace {

	n1 = namespace {
		n2 = namespace {
			printf = (format string, args ...) u32;
			bar = () {};
		};


		n2::bar();
	};

    asas = 'asas';
    pow = (a f64, b f64) f64;

	foo = () {};

	exp = namespace {
		print_hello = () {
    	    //foo('hello goga\n');
	    };    
    };

	//exp = 'aaaaaa';
};
