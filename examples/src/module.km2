

namespace {
    exp namespace to_export {
    	printf = (format string, args ...) u32;
    	const my_cst = 'asas';
    	pow = (a f64, b f64) f64;
    };

    print_hello = () {
        //printf('hello goga\n');
    };
}
